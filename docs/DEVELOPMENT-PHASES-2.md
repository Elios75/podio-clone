# Development Phases 2 — Gap Analysis & Roadmap (Phases 8–14)

Written after completing Phases 0–7. Every item below is in `podio-clone.md` (the spec) but not yet built, or built as a v1 slice that lacks spec depth. Ordered so that gaps in *existing* features close before new surface area opens.

---

## Working notes (read me first, next session)

- **Session handoff (2026-07-17, API + importer pass)**: migrations now through **85**. (1) **User menu + profile**: the global-bar avatar opens a Podio-style menu (`user-menu.tsx`) → NEW `/profile` (photo upload to the public `avatars` bucket, migration **77**, own-folder storage policies; display name) and `/account` (email change via confirmation link, password change); Pricing/Billing/Batch jobs/My shared apps are disabled placeholders. Profile Save closes back via router.back(). Null `full_name`s were the cause of "Someone" everywhere — backfilled from email; signup still doesn't set it. (2) **Activity feed rebuilt Podio-style**: grouped per item, fully expanded — creator avatar, teal title, "«ItemName» by X, 2 months ago · App · Comment" meta, attachment chips, sub-activity lines, inline last-3 comments + ALWAYS-open Add-comment (`feed-comment.tsx` → add_comment RPC), relative `timeAgo`. (3) **Canvas**: activity column hideable (ephemeral — always visible on fresh load), dashboard grid 2-or-3 columns (persisted in the synced layout), embed TABS above the dashboard hold ANY tile kind via the standard picker (migrations **78+79**, `workspace_embeds` kind/config; iframe tabs render viewport-height); Google Sheets embeds default to `/htmlembed` grid with a Grid|Preview fallback toggle (`sheetVariants` in iframe-tile). REPO-WIDE BUG fixed: Tailwind `calc()` values need `_` for spaces — `h-[calc(100vh-15rem)]` is INVALID CSS silently ignored (iframes collapsed to 150px); four occurrences fixed. (4) **API platform (migrations 80–84, five parallel agents)**: **OAuth2** (`/api/oauth/token` + `/oauth/authorize` consent + revoke; grants authorization_code/refresh_token/app + password handled in the route via signInWithPassword; every access token is MIRRORED into api_keys so all /api/v1 endpoints accept OAuth bearers unchanged; expiry enforced by rotation/revocation — v1 tradeoff; login gained safe `?next=` handling). **Hooks API** (`/api/v1/hooks` CRUD + verify lifecycle + deliveries diagnostics; field-level + space-level hooks; new DB-trigger events comment.delete/app.update/app.delete/tag.add/tag.delete/file.change/form.*/member.*; shared `emit_webhook_event` fan-out). **Items depth** (revisions/diff/revert via cumulative `revision_state` reconstruction — revision rows are NOT full snapshots; clone impersonates the key's owner via `set_config('request.jwt.claims',...)`; surgical `values/{fieldId}` PUT; bulk soft-delete ≤100). **Flows/subscriptions/notifications API** (`/api/v1/flows` CRUD/activate/possible-attributes/effect-attributes validated against the 7 trigger + 8 action types; follows + notifications as the key's owning user). **Pattern**: api_request is NEVER modified — each slice ships its own SECURITY DEFINER dispatcher (hooks_api/items_api/flows_api/import_api) replicating key+scope+rate validation inline. Rate-limit HEADERS on every /api/v1 response via `api_rate_status` (migration 84) in api-auth.ts. `/developers` rebuilt as a 6-page docs hub (areas, tutorials, JS/Python/PowerShell SDK patterns, curl test-console blocks). (5) **Podio workspace importer (migration 85)**: `scripts/podio/` — refresh-token client with 1.2s throttle (Podio: 1000/h, 250/h heavy), `list-spaces`, `fetch-fixtures` (fixtures gitignored — real client data), fixture-tested `mapper.mjs` (type table incl. app→relationship two-pass, Podio quirks: `settings.apps` key, un-prefixed hex colors, space-contacts drop with notes, html→plain, calculation imported inactive with original script), `import-space.mjs` runner → idempotent `import_api` dispatcher (import_map per org, import_runs progress, always-fresh target workspace) + `/org/:org/import` live status page + `docs/PODIO-IMPORT.md`. **Proven**: Demo Workspace (3281781) imported 129/129 items, 5 apps, 35 fields, 19/19 refs, 9 files-as-links; values verified in native shapes. Env: PODIO_CLIENT_ID/SECRET/REFRESH_TOKEN + CLONE_API_KEY in .env.local (api_keys row 'podio-importer', write scope, customers1 org). **Hygiene open items**: regenerate the Podio client secret + Tasks app token (both appeared in screenshots) and the user's Podio password if it was typed into grant_type; production import (e.g. ADS and MACS 7680828) pending — takes hours under Podio's 250/h item limit.

- **Session handoff (2026-07-17, dashboard pass)**: Workspace dashboard rebuilt into a configurable Podio-style surface; migrations now through **76**. (1) **Tile picker** (`tiles/add-tile-modal.tsx`, replaces the inline creator in `dashboard-tiles.tsx`): Podio's three-tab modal — *Overviews* (Workspace Tasks / Calendar / Files / Contacts overview tiles + Text tile), *Apps* (searchable app list → tile with the app's 5 newest records), *Reports & Charts* (app → Count/Sum/Average/Grouped config). Migration **76** widens `dashboard_tiles.kind` (adds app/tasks/calendar/files/contacts/text/iframe/youtube) and makes `app_id` nullable. The server page (`page.tsx` tiles loop) computes per-kind data: overview lists (files get signed URLs), app items with hrefs, config passthrough for text/embed kinds. Tile bodies live in `tiles/overview-tiles.tsx`. Built by four parallel subagents on isolated files + one integration pass. (2) **Beyond-Podio embed tiles**: `tiles/iframe-tile.tsx` — sandboxed Web Embed with `normalizeEmbedUrl` (https-only, auto-converts Google Docs/Sheets/Slides `/edit` and Drive `/view` links to `/preview`, refuses non-http schemes, permanent "Open ↗" fallback because X-Frame-Options blockage is undetectable client-side); `tiles/youtube-tile.tsx` — lite click-to-play player (`parseYouTubeId` handles watch/youtu.be/shorts/embed/live, 11-char validation, thumbnail-first, `youtube-nocookie` iframe only after click). iframe/youtube/grouped tiles span 2 grid columns. (3) **Reports can aggregate table-field columns**: the number-field picker also lists numeric columns inside table fields as composite ids `fieldId:columnId` (label "Invoices → Amount"), split on save into config `number_field_id` + `table_column_id`; sum/avg/grouped in `page.tsx` sum `value.rows[colId]` per item (this is how "Total Invoices" works when amounts live in an embedded table). (4) **Panel board = free grid** (`panel-board.tsx` rewrite): 6-unit CSS grid, per-panel width snaps ⅓/½/⅔/full + free px height (content scrolls; hairline closes the clipped edge; dragging past the content bottom releases height to auto), Podio-style corner resize handle on hover, drag-anywhere reordering with 2D insertion index. Overlap bug fixed: grid items need `min-w-0` + horizontal clipping or narrow panels overflow under their neighbor. (5) **Layouts are ACCOUNT-SYNCED** (migration **75** `workspace_panel_layouts`, one row per user per workspace, RLS self-owned): the server page seeds the first render (no flash), every change upserts, legacy localStorage layouts are adopted once; last-writer-wins across simultaneous sessions. (6) Smaller: App Market category labels humanized via `market/category-label.ts` (slugs stay stable keys; CRM/HR uppercased), app tab bar inactive icons darkened to #4E5E5E, `tsconfig.tsbuildinfo` gitignored + untracked, dashboard tile chrome converged from legacy slate/blue to podio tokens.

- **Session handoff (2026-07-17)**: Workflow changed + four features + 21 market templates; migrations now through **74**. (0) **Claude now typechecks/commits/pushes directly** via UNSANDBOXED local git (`dangerouslyDisableSandbox`) — push auth is Windows Credential Manager, non-interactive; the old index.lock / CRLF-noise / phantom-tsc problems were all sandbox-mount artifacts. The user no longer pushes manually. Also hard rules recorded in Claude's memory: the ONLY Supabase project Claude may touch is Podio-Clone `gwtfthmlwtylefvfklht` (explicit project_id on every call); FuelTrack (`bonjyxumwuokdwlrcxwx`) and the Aeries-mirror connector are strictly off-limits; commands handed to the user must be PS 5.1 one-liners (`;` separators, never `&&`). (1) **Inline app-scoped top-bar search** (migration **53** `search_app` RPC — SECURITY INVOKER, items-by-title + field values + comments for ONE app, ILIKE type-ahead fallback, deduped per kind+href): the upper-right magnifier now expands in place into a white rounded pill (`global-search.tsx` in the GlobalBar), placeholder "Search in {App}" when the URL is inside an app (RLS-confirmed lookup; ws-level routes fall back to global `search_all`), live dropdown (250ms debounce), Enter → first result, Escape/✕/outside closes, "Search everywhere" footer link → the kept /search page. (2) **Podio-style configurable Card layout** (page.tsx §view==="board"): cards now render title (ink, 17px), grey excerpt block (first text card-field, "«Label» not set" italic when empty), label-LESS value stack (chips, avatar+name for contacts, "not set" placeholders), and a "date by creator · 💬 count" footer (creator + comment counts fetched only when Card renders); 3-up grid on xl. WHICH fields show: `?cardf=` > active view `settings.card_fields` > all non-primary fields — edited via a "Card fields" checkbox cluster in the toolbar filter panel (view-toolbar.tsx, Card layout only) and captured on view save (views-pane.tsx). (3) **Cross-workspace relationship targets**: both builders now list every org app the user can see as "Workspace / App" (new-app/page.tsx, edit/page.tsx→relAppChoices), and the template editor gained a "Links to" picker per relationship field — fixing a REAL latent bug where every editor publish rebuilt config from scratch and silently WIPED `related_app_id` (update_app_schema replaces config wholesale; app-editor now re-emits it). Item detail already showed incoming/outgoing Related items cross-workspace, so task-style records linked to any record now surface there. (4) **App Market catalog: 21 public templates** (migrations **54–74**, all idempotent-by-name, org_id null): Task Manager, Meetings, Projects, Timesheets, Expenses, Contacts, Audits, Appointments, Communication Log, Work Orders, Cases, Annual Goals, Grant Tracker, Milestones, To-Dos, Events, Ideas, Action Plans, Tickets, Expense Reimbursement, Surveys (25 public total incl. the four migration-41 industry packs) — each with status-first single-select categories (kanban groups by FIRST single-select), 3-4 views, 0-2 automations (item_created/item_updated + add_comment/create_task only), 2-3 sample items (no contact/relationship/file values — they need real ids), and a relationship field whose help_text/usage_instructions tell the installer to point it at a target app via Modify Template (any workspace). Batches of 5 were authored by PARALLEL subagents from a shared constraints prompt with migration 54 as the canonical pattern, then reviewed/applied/pushed by the orchestrator — reuse that flow for future batches, and offer the handoff write-up at the end of each batch. **Template-system limits (open)**: install ignores view filters; date-reached triggers can't be templated (trigger jsonb isn't external_id-translated); settings.card_fields/group_field_id can't be pre-seeded (field ids unknown at install). Env: `NEXT_PUBLIC_INBOUND_DOMAIN` documented in .env.local.example. Session earlier confirmed: /search page kept as deep-search surface; .gitattributes still pending (renormalization commit — Claude can now run it directly when approved).

- **Session handoff (2026-07-07)**: Four features shipped on top of the 2026-07-06 state; migrations now through **52**. (1) **Kanban "Board" — 6th layout**: revived the orphaned `board-view.tsx` into the layout switcher (Badge · Table · Card · Activity · Calendar · **Board**). Migration **51** (`20260706000051_podio_kanban_layout`) adds `kanban` to the `view_layout` enum so saved Board views persist (enum was table/card/calendar/badge/stream/dashboard). `[appSlug]/page.tsx` resolves the group field (`?group=` > active saved view `settings.group_field_id` > first **single-select** category field; multi-select is excluded because a drag writes ONE value and would clobber the others), builds color-resolved options + per-item card buckets, and disables the Board toggle when no single-select category exists. `board-view.tsx` = optimistic drag (local state, reverts + shows error on failure), a group-by selector when >1 single-select category, colored-dot column headers + counts, trailing "No value" column. The drag does a **surgical single-field upsert/delete on `item_field_values`** — deliberately NOT `save_item` (which deletes+reinserts ALL of an item's values and would wipe every other field); trade-off is a kanban drag does NOT fire activity/automations (accepted v1 limit). `view-toolbar.tsx`: Board uses the `board` glyph, Card moved off it to the `layout` glyph so they don't collide. (2) **Comment icons**: `[itemNumber]/comments-section.tsx` now renders PodioIcon **pencil/trash/heart** in the comment header (matching real Podio); the three-emoji reaction row was consolidated into a single heart mapped to the `❤️` reaction (old 👍/🎉 buttons removed — existing ❤️ reactions display as likes, data preserved). (3) **Record "Actions" menu**: migration **52** (`20260706000052_podio_item_actions`) adds three SECURITY DEFINER, membership-checked RPCs — `clone_item` (deep-copies field values + relationships in SQL, so no client value-shape reconstruction; recomputes calcs, auto-follows, logs `item_created` with `cloned_from`), `delete_item` (SOFT: `is_deleted`/`deleted_at` + `item_deleted` activity), `recalc_item` (wraps `compute_calculations`). New `[itemNumber]/actions-menu.tsx` replaces the dead "Actions ⌄" button in `[itemNumber]/page.tsx`: **Print** (`window.print`), **Clone** (→ redirect to new item), **Email to item** (shows/auto-creates an `app_email_addresses` row; item address = `base+i{itemNumber}@domain`; domain from `NEXT_PUBLIC_INBOUND_DOMAIN`, default `inbound.example.com`), **Download all files** (downloads each attached file individually via signed URLs — no zip lib installed), **Developer Info** (modal: item/app ids, item number, timestamps, `/api/v1/items/{id}` URL, copyable), **Refresh Calculations**, red **Delete** (confirm → soft delete → back to app). (4) **Table field resizable + reorderable columns**: added `width?: number` to `TableColumn` (`src/lib/fields.ts`); extracted the form table grid into `[appSlug]/table-field.tsx` with pointer-capture column resize (mirrors `sheet-table.tsx`) + HTML5 header-drag reorder, BOTH persisted to `app_fields.config.columns` (order + per-column `width`) via a DIRECT `app_fields` update — RLS `p_app_fields_write` = `can_edit_items`, so this is **definition-level** (affects every user, every record, and the template editor). Row data stays keyed by column **id** so reorder never rewrites a cell; the builder round-trips unknown config keys so `width` survives publish. `item-form.tsx` now renders `<TableField/>` (dropped now-unused imports currencySymbol/tableColumnTotals/TableRow). **Gotchas confirmed this session**: `save_item(p_app,p_item,p_values)` DELETES all of an item's field values then reinserts only the fields in p_values — single-field writes must never use it; `app_fields` writes are gated by `can_edit_items` (not admin); `activity_events.event_type` is free `text` (no enum); a read-only `git status` run in the SANDBOX leaves a stale `.git/index.lock` the sandbox can't unlink → clear with `Remove-Item C:\Users\fd\podio-clone\.git\index.lock -ErrorAction SilentlyContinue` and NEVER run git in the sandbox; the 100+ "modified" files git shows in the sandbox are CRLF↔LF line-ending noise, not real changes. **New open items**: add `NEXT_PUBLIC_INBOUND_DOMAIN` to `.env.local.example`; add a `.gitattributes` (`* text=auto eol=lf`) to silence the CRLF sandbox noise; pre-existing harmless unused `FORM_GRID_COLS` import in `item-form.tsx` (noUnusedLocals is off, so no tsc failure).

- **Session handoff (2026-07-06)**: UI parity campaign continued + major features. Shipped this session: draggable workspace panels (panel-board.tsx, localStorage layout); App Market converged to skill §11; **beyond-Podio features**: 1/2/3-column app layouts with per-column drag + full-width separators (apps.layout_settings.columns + config.column per field, splitSections in lib/fields.ts), **table field** (embedded one-to-many rows; config.columns schema, value jsonb {rows:[...]}, totals footer, "N rows · $X" sheet summary — migration 50); Done=Publish in template editor; layout picker saves instantly; direction-aware separator drops (top half=before, bottom=after); Create App modal now creates app directly (Title field only) → template editor; sheet view: click-to-sort headers + drag-resizable columns (sheet-table.tsx hybrid client/server table, widths in localStorage podio.sheet-widths); layout switcher dropdown on toolbar grid icon (Badge/Table/Card/Activity/Calendar — current Podio naming); Card layout = record-card grid (kanban board-view.tsx orphaned, kept in repo); **typography = system-ui stack** (Segoe UI on Windows — modern Progress Podio ships NO webfont; Source Sans removed, src/app/fonts/ deleted); compact app tab bar (h-5 icons, 72px tabs); Podio-style /tasks page (tabs, composer, Overdue groups, Labels rail); GlobalBar extracted (src/components/global-bar.tsx, left slot + activeTool + user + initialUnread) — **chrome NEVER disappears**: all standalone pages (/home /tasks /search /calendar /messages /notifications) chromed via getGlobalChrome() (src/lib/global-chrome.ts) + OrgPickerDrawer (☰ slide-over listing all orgs/workspaces); top-right cluster: help/search/avatar/NotificationsBell (red dot + yellow pill, dropdown, realtime + refetch fallback)/ChatLauncher → chat slide-over with **presence** (use-presence.ts, Supabase Realtime presence channel "online", orange dot = online) + 1:1 threads (lazy conversation creation).
  **DATABASE MOVED**: dedicated Supabase project **Podio-Clone (id gwtfthmlwtylefvfklht)** — all 42 migrations applied, pg_cron/pg_net enabled + 10 cron jobs re-registered, both edge workers deployed (verify_jwt=false), `podio` schema exposed to PostgREST via `alter role authenticator set pgrst.db_schemas` + reload. .env.local points here. The old FuelTrack project (`bonjyxumwuokdwlrcxwx`) is an UNRELATED project and is strictly OFF-LIMITS — never run SQL, migrations, or any change against it (the previously-floated `drop schema podio cascade` cleanup is cancelled; the leftover schema stays as-is). Auth hooks: only before-user-created enabled (password-verification hook is plan-gated; app-layer sso_domain_lookup check covers it). Worker/vault secrets (Twilio/VAPID/Resend) NOT yet set on new project. DB is fresh — user account fernan@360isuite.com, orgs Customers/Customers1, minimal test data.
  Env quirks: sandbox mount serves stale/truncated copies of recently-modified files — Read tool (Windows paths) is authoritative; never repo-wide tsc in sandbox (phantom errors); dynamic Tailwind classes don't compile; JSX crossing server→client props needs explicit keys; `if (!user) redirect()` narrowing doesn't reach hoisted functions (capture const userId). Known open bug: Next.js 15.5.20 dev overlay "frame.join is not a function" masks real errors — upgrade `npm install next@15` pending. Next candidates: kanban board as 6th layout, reports/"Create report", pgTAP RLS tests, /messages deep-parity.

- **Session handoff (2026-07-04)**: Phases 8–14.5 all shipped (migrations 0–41 applied; sms-worker + push-worker edge functions deployed). Current focus is **Podio UI parity**, driven by the `podio-design` skill (source: `docs/design/podio-design-skill/` — read it before touching any UI). Done: drawer nav, full-width views pane (Team/Private tabs, +Add with group-by, per-view counts, category dot sub-rows), aligned white content surface, record view (header bar + Activity/Comments rail), item creation view, all modals (create-workspace→invite, add-app chooser, Create New App), PodioIcon line-icon system (~46 app + chrome icons, emoji→icon mapping + name inference), self-hosted Source Sans 3 (`src/app/fonts/`), wrench mega-menu (APP/DATA/ACTIONS, working Clone app), Modify Template editor (fields palette + canvas + full drag-and-drop). Dev runs on Turbopack (`npm run dev`); the recurring "reading 'call'/'map'" dev errors are stale-chunk artifacts — fix with `Remove-Item -Recurse -Force .next` + restart. **Possibly unpushed**: template-editor DnD + mega-menu batch — check `git status`. Next candidates: /market + /messages convergence (specs in skill §11/§13), reports/"Create report", pgTAP RLS tests.

- **Personal project mode**: billing limits are intentionally disabled — both orgs are set to the `enterprise` plan (unlimited). Ignore plan-limit concerns until commercialization; the Stripe/billing machinery is built but dormant (env-gated).
- Migrations through **52** are applied to the dedicated Podio-Clone project (`gwtfthmlwtylefvfklht`); the old FuelTrack project (`bonjyxumwuokdwlrcxwx`) is unrelated and off-limits — never touch it. Repo migration files are kept in sync for fresh installs. Use the Supabase connector for migrations/SQL.
- Workflow: edit files in `C:\Users\fd\podio-clone` directly, apply migrations via the Supabase connector (ONLY project `gwtfthmlwtylefvfklht`), then Claude typechecks + commits + pushes via **unsandboxed** local git (verified 2026-07-17: `credential.helper=manager` authenticates non-interactively; the old index.lock/CRLF-noise/phantom-tsc problems were all sandbox-only artifacts — never run git or repo-wide tsc sandboxed). The user no longer pushes manually.
- `.env.local` exists locally (gitignored) with the Supabase URL + anon key. Optional keys documented in `.env.local.example` (Turnstile, Stripe, Anthropic).

---

## Phase 8 — App builder completion (the biggest gap)

The builder is create-only. Podio's core promise is *modify anytime*.

- **Edit app schema after creation**: add/rename/retype/delete fields on a live app; soft-delete keeps existing values (`app_fields.status='deleted'` already supported by every query).
- Warning flow when changing/removing fields that hold data (count values, confirm).
- Snapshot each publish to `app_schema_revisions` (table exists, never written) + schema history viewer.
- Builder inputs that exist in schema but not UI: help text, description, default values, hidden / hidden-if-empty toggles.
- Drag-and-drop field ordering (dnd-kit; replaces arrows).
- App settings editing: name, icon, description, usage instructions, item name, archive/delete app with confirmation.
- Field-type depth: multi-select category; multiple values for phone/email (positions exist); date end + time; money currency list per field; link previews/embeds.
- **Calculation engine v1**: same-item formulas over number/money/progress fields, evaluated in `save_item`, stored as normal values (spec §2.12). Rollups from related items follow in Phase 9.
- Enforce spec limits (§20): max fields/app, options/field — cheap trigger checks.

## Phase 9 — Relationships & views depth

- **Related-items section on item detail** (reverse relationships — `item_relationships` has the data; nothing displays it).
- Multi-value relationship fields; restrict selectable items by saved view; cross-workspace picker.
- **Rollup/aggregate over related items** (sum of deal values on a company).
- Relationship map visualization (simple graph of app-to-app links).
- Table layout: show/hide/reorder/resize columns persisted to `app_views.columns` (column exists, unused); inline cell editing.
- Badge and stream layouts (last two of the six spec layouts).
- Group/sub-group in table views; default view per app; view ordering; last-used view memory.
- **View-filter → SQL translation** — the scale prototype flagged since Phase 3; required before any app holds >500 items.

## Phase 10 — Tasks, calendar & collaboration depth

- Task labels UI (schema since day 1), repeating tasks (materialize from `repeat_rule` via the existing cron), reminders (queue notifications at `reminder_at`), task comments/files (polymorphic tables ready), workspace task lists, task filters.
- **Personal calendar**: tasks + items across all apps/workspaces; workspace calendar; week/day views; per-app colors; **ICS feed** (signed URL) for Google/Outlook subscription.
- Status posts UI on workspace streams (schema only today); like/react on activity; feed filters; mute workspace from home stream (`follows.muted` is wired for it).
- Comment depth: revision history, convert-comment-to-task.
- Notification preferences UI (`notification_prefs` jsonb exists) + daily digest (cron assembles, outbound queue sends).

## Phase 11 — Email & files, production-grade

- **Wire outbound delivery**: worker (edge function or route + cron) draining `outbound_emails` through Resend; covers automation emails, notification digests, share invites.
- Inbound live: point provider at `/api/inbound-email`; reply-to-item threading (plus-addressing on item comments → comment instead of new item); attach inbound files.
- Send email from an item; `email_templates` UI (table exists).
- **Storage hardening**: private bucket + signed URLs (current bucket is public — flagged since Phase 2), per-plan size limits, storage quota tracking per org.
- File versioning UI (`previous_version_id` ready), workspace files page, external-link files (Drive/Dropbox URLs as `provider` rows — full pickers in Phase 13).

## Phase 12 — Automation & platform depth

> **Status: Phase 12a shipped** (migration 35): date-reached / comment-added / task-completed / manual triggers; http_request + update_related_item actions; dry-run test mode; run-log detail viewer.
>
> **Phase 12b shipped** (migration 36): webform depth (theme/custom CSS, redirect URL, URL-param prefill via external_ids, embed snippet, allowed-domains stored); API v1.1 (per-key rate limiting w/ 429s, automatic webhook verification handshake, /api/v1/workspaces + /api/v1/tasks endpoints, hosted docs at /developers); marketplace depth (ratings + reviews UI, install with sample data, version bump on re-save, public publishing flow); XLSX import/export via SheetJS (export button + both import flows accept .xlsx). Remaining in Phase 12: scheduled flows, inbound-webhook trigger, PDF/approval/loop actions, flow versioning UI, form captcha + file uploads, embed-domain enforcement, org backup export.
>
> **Phase 12c shipped** (migration 37): scheduled flows (hourly/daily/weekly cron over condition-matched items); inbound-webhook trigger (token-secured POST /api/hooks/:id creates item + fires flow); generate_pdf action + /api/pdf/:itemId renderer (pdf-lib); approval steps (flow pauses on an approval task, resumes on completion) and loop-related steps in advanced flows; automatic flow versioning with history/restore UI; per-form Turnstile captcha (verified in /api/forms/submit; note: direct RPC path bypasses captcha until a service-role gate exists); org backup export (admin JSON snapshot, audit-logged). Phase 12 is complete except: public form file uploads, embed-domain enforcement (frame-ancestors), captcha RPC-bypass hardening.

- Triggers: **date-reached** (cron scans date fields), scheduled flows, task-completed (RPC exists, no automation hook), comment-added, manual "run now" button, inbound-webhook trigger.
- Actions: HTTP request (via `pg_net`, mirroring webhook delivery), **PDF generation** from item + template, approval step (approve/reject task gating the flow), update-related-item, loop over related items.
- Test mode (`automation_runs.is_test` exists), run log detail viewer, flow versioning UI (`automation_revisions` table waiting).
- Webform depth: theme/custom CSS, redirect URL, captcha (Turnstile), embed snippet + allowed domains, URL-parameter prefill (spec §8 — `external_id`s were designed for this), file uploads on forms.
- API v1.1: automatic webhook verification handshake, per-key rate limiting, hosted API docs page, workspace/task endpoints, service-user keys.
- Marketplace depth: install with sample data, template versioning, ratings UI (`template_reviews` exists), public publishing flow.
- XLSX import/export (SheetJS) + background jobs for large files; org backup export.

## Phase 13 — Integrations & billing

> **Status: Phase 13a shipped** (migration 38): billing plans (free/team/business/enterprise) with enforced limits — users, items, storage, automations/month via DB triggers + bounded counts; usage dashboard + upgrade UI on the org page; Stripe Checkout + signature-verified webhook (env-gated, manual set-plan fallback for dev; Covantia org grandfathered to business); chat_message action posting to Slack/Teams incoming webhooks; external file links (Drive/Dropbox/OneDrive URLs) attachable to items; plan-based retention cron (runs, webhook deliveries, item revisions, daily 03:30 UTC); Zapier/Make/Stripe recipes on /developers.
>
> **Phase 13b shipped** (migration 40): Twilio SMS — `send_sms` action queues `outbound_sms`, cron pings the `sms-worker` edge function (deployed; form-encoded Twilio API + Basic auth, which pg_net can't do), env-gated via Vault `sms_worker_url`/`sms_worker_token` + edge secrets `TWILIO_*`/`SMS_WORKER_TOKEN` (docs/SMS.md); OAuth file pickers on item detail — Google Picker, Dropbox Chooser, OneDrive picker (client-side SDKs, links stored as external `files` rows; env-gated per provider, see .env.local.example); hard SSO enforcement — `hook_password_verification` + `hook_before_user_created` Auth Hook functions (must be enabled by hand in Dashboard → Auth → Hooks, docs/SSO.md); IdP group→role mapping — `security_settings.sso_group_roles` editor on the org page, `claim_sso_membership()` applies it authoritatively each login (owners never touched). Remaining in Phase 13: calendar push sync.

- File pickers: Google Drive, OneDrive, Dropbox (attach as external files).
- Calendar sync out (ICS from Phase 10 → push integration), Slack/Teams notification channel (webhook consumer recipes), Zapier/Make templates over the public API, Twilio SMS action.
- **Billing**: Stripe subscriptions mapped to `organizations.billing_plan`; plan-gated limits (users, items, storage, automations/month) enforced by the existing limit checks; upgrade UI. (Free/Team/Business/Enterprise from spec §19.)
- Hard SSO enforcement via Supabase Auth Hook; IdP group → role mapping.
- Retention policies: revision pruning + file cleanup by plan (cron).

## Phase 14 — Differentiators (spec §23)

> **Status: AI app builder shipped** (migration 39): describe a workflow → Claude generates a definition (app/fields/views/automations with external_id refs) → preview → ai_install_app() installs it and keeps a private template as provenance. Workspace page: /org/:org/:ws/ai-builder (needs ANTHROPIC_API_KEY).
>
> **Phase 14 shipped** (migration 41): calendar sync out — Add to Google/Outlook/Apple-webcal subscribe buttons on /calendar (closes Phase 13); AI formula builder (/api/ai/formula + ✨ button in both builders' calculation config) and AI automation suggestions (/api/ai/suggest-automations + suggestion panels on the automations page), both ANTHROPIC_API_KEY-gated; white-label portals v1 — organizations.branding jsonb + BrandingSection on the org page, public /portal/:orgSlug (portal_lookup RPC, my_shared_items for signed-in guests), PORTAL_DOMAINS env-gated custom-domain rewrite (docs/PORTALS.md); relationship map — /org/:org/:ws/map server-rendered SVG of app-to-app links; template installs now create definition automations (shared install_definition_automations; ai_install_app deduped); 4 industry starter packs seeded public (Recruiting, Client Onboarding, Field Service, Purchase Approvals — with sample items + automations); PWA depth — sw.js (offline fallback, cache strategy, push display), /offline, create-mode item drafts in localStorage with restore bar, 📷 camera capture on image fields, push_subscriptions + pushed_at + push-worker edge function (deployed; VAPID env-gated, docs/PWA.md). Remaining in Phase 14: none — differentiator backlog moves to spec §23 wishlist (AI dashboards, offline queue for edits).
>
> **Phase 14.5 shipped (UI convergence, core journey)**: Podio design language applied per the `podio-design` skill (source: docs/design/podio-design-skill/). Foundation: `podio` color family in tailwind.config.ts, Source Sans 3 via next/font, page bg `podio-page`. Converted: org shell (grey-teal global top bar + restyled workspace sidebar), shared AppTabBar (icon-over-label tabs, active = white card) on workspace + app pages, app page rebuilt as Podio two-zone body (white left views pane with teal app title + view rows, toolbar with orange active-layout pill + teal Add button, sheet with row numbers/pastel option-color chips, card/board/calendar restyles), item detail, tasks, calendar, notifications, home, login. Core-journey files verified free of legacy blue; still-blue elsewhere (builders, settings, marketplace, developers) converges page-by-page as touched — the skill triggers automatically on any podio-clone UI work.

- **AI app builder**: describe a workflow in chat → generated app + fields + views + automations (Claude API; the `app_templates.definition` format is the generation target — this is why it's JSON).
- AI formula builder for calculation fields; AI workflow suggestions.
- White-label client portals (custom domain + logo per org, guest-facing views).
- Better relationship diagrams, approval-flow templates, industry starter packs seeded into the public marketplace.
- Mobile: real service worker (offline drafts), push notifications, camera capture into image fields.

---

## Cross-cutting debt to schedule alongside (not a phase)

- **UI/permission polish**: hide controls light/guest members can't use (backend enforces; UI still shows buttons that fail).
- Generated TypeScript types (`supabase gen types --schema podio`) replacing the `as any` casts accumulated across pages.
- Error toasts + optimistic updates instead of full `router.refresh()` round-trips.
- Test suite: pgTAP for RLS policies (highest value per line of test code in this codebase), Playwright happy paths.
- Tighten `user_profiles` visibility to shared-org members before multi-org production use.

## Suggested order

8 → 9 → 10 → 11 → 12 → 13 → 14. Phases 8–10 make existing surface spec-complete (roughly one session each per phase half, as before). Phase 11 is the production-readiness gate. Phases 12+ are expansion. The AI builder (14) can be pulled forward any time after 8 — it only depends on the template format, which is stable.
